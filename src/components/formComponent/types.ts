export interface InputTextProps {
    labelText : string;
    helperText: string;
    value: string;
    id: string;
}

export interface CheckboxProps {
    labelText: string;
    id: string;
    helperText: string;
}

export interface LinkProps {
    text: string;
    href: string;
};

